
<p style="color: green"><%= notice %></p>


<% content_for :title, t(".title") %>
<h1><%= t(".title") %></h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      
      <%= link_to t(".show_link"), category %>
    </p>
  <% end %>
</div>


<%= link_to t(".new_link"), new_category_path %>